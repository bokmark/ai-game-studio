> 中文翻译 | [English](../../../.claude/rules/design-docs.md)
> 同步基线:commit `41d7e09`(2026-07-29);如有出入以英文版为准。英文版含 paths frontmatter(路径作用域声明),中文版从略。

# 设计文档规则

- 每份设计文档必须包含以下 8 个章节:概述(Overview)、玩家幻想(Player Fantasy)、详细规则(Detailed Rules)、公式(Formulas)、边界情况(Edge Cases)、依赖(Dependencies)、调优旋钮(Tuning Knobs)、验收标准(Acceptance Criteria)
- 公式必须包含变量定义、预期取值范围和示例计算
- 边界情况必须明确说明会发生什么,不能只是「优雅处理」
- 依赖必须是双向的——如果系统 A 依赖 B,B 的文档必须提及 A
- 调优旋钮必须指定安全范围及其影响的玩法方面
- 验收标准必须可测试——QA 测试员必须能够判定通过/不通过
- 不允许含糊其辞:「系统应该手感好」不是有效的规格说明
- 平衡数值必须链接到其来源公式或依据
- 设计文档必须增量编写:先创建骨架,然后逐节填写,每节之间需用户批准。每个获批章节立即写入文件,以持久化决策并管理上下文
