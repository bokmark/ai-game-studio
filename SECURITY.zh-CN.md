> 中文翻译 | [English](SECURITY.md)
> 同步基线:commit `0de6c40`(2026-07-29);如有出入以英文版为准。

# 安全策略

## 支持版本

只有 `main` 分支会收到安全修复。Fork 和旧版本不在支持范围内。

## 报告漏洞

**请勿通过公开的 GitHub issue 报告安全漏洞。**

请使用 GitHub 的私密漏洞报告功能:

**[报告漏洞 →](https://github.com/bokmark/ai-game-studio/security/advisories/new)**

请尽可能提供细节:
- 漏洞描述及其影响范围
- 复现步骤
- 潜在影响与攻击场景
- 任何建议的缓解措施

**你可以期待的响应:**
- **48 小时**内确认收到
- **7 天**内给出状态更新
- 已确认的漏洞在 **90 天**内解决

## 哪些在范围内

AIGS 是一个**本地开发工具**——它安装 shell 钩子并协调直接运行在你
机器上的 AI 代理。安全问题主要关乎:被贡献的代码在用户不知情的情况
下在其环境中执行。

### 高严重度
- 钩子(`.claude/hooks/*.sh`)在用户机器上执行恶意或未披露的 shell 命令
- 技能或代理窃取环境变量、API 密钥或机密信息
- 通过技能或代理定义进行 prompt 注入,导致 Claude 绕过安全措施或
  执行未经授权的破坏性操作
- 以用户无法审计的方式悄悄改变行为的贡献

### 中严重度
- 技能发起未披露的对外网络请求
- 代理定义提升权限或绕过用户确认提示
- 钩子在不同平台上行为不一致以掩盖行为
- 技能在没有明确用户批准步骤的情况下,写入其文档声明范围之外的位置

### 范围之外
- Claude 或 Claude Code CLI 本身的行为
  (请报告给 [Anthropic](https://www.anthropic.com/security))
- 用户自己的 Claude Code 安装或编辑器扩展中的缺陷
- 没有现实攻击路径的理论漏洞
- 需要物理接触用户机器的问题

## 贡献者安全准则

贡献钩子、技能或代理时:

- **钩子必须 POSIX 兼容** ——使用 `grep -E`,不用 `grep -P`;避免
  在不同操作系统上行为不一致的平台相关语法
- **不做静默网络调用** ——钩子或技能不得发起网络请求,除非明确
  写入文档且由用户选择开启
- **不读取机密或环境变量** ——超出最小必要范围的一律不读,且所需
  部分必须在技能头部清楚写明
- **技能不得写超出其文档声明范围的位置** ——除非有明确的用户确认步骤

## 披露政策

我们遵循 **90 天协同披露** 时间线:

1. 你私下提交漏洞
2. 我们在 48 小时内确认收到
3. 我们在 7 天内确认并评估严重度
4. 我们开发并测试修复
5. 任何公开披露前我们会先通知你
6. 公开披露在修复发布后进行,或在 90 天时进行——以先到者为准

除非你希望保持匿名,我们会在发布说明中为报告者署名。
